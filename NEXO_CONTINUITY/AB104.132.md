# AB104.132 — effect-adapter source integrity audit — 2026-09-26

Previous: AB104.131.

Audit finding:
- While auditing the adapter/runtime boundary, the canonical `src/nexo/effect-adapter.js` fetched from GitHub contained literal `\\n` escape sequences embedded in source text at multiple statement boundaries rather than actual line breaks.
- This is a source-integrity defect independent of the concurrency design. If interpreted literally by the JavaScript parser, those sequences can produce invalid source syntax.
- The defect was visible directly in the canonical file content; it was not inferred from a test failure.

Repair:
- Normalized the literal `\\n` sequences in `src/nexo/effect-adapter.js` to real newlines without changing the adapter logic.
- No historical failure evidence was removed.

Persistence:
- Repair commit: 6afacb2e6cf0d62117de82f3548c3fd747bb186f
- Previous runtime convergence chain remains intact:
  - 74a7d2ecac1e920db47e198b43b52e113399f353
  - bf0992a4be8e4ed9963759339eacd0ad19e76163
  - c30fa3aeadd89dad10fd86f6ef04d7000d2af5cb

Validation:
- Fresh workflow lookup for repair commit reports no workflow run through the available GitHub workflow interface.
- Therefore this repair is PERSISTED but NOT FRESH-CI-VALIDATED.

Research note:
- MDN confirms that async functions suspend at `await` and resume later, allowing other async work to interleave; Promise composition can explicitly serialize or coordinate asynchronous operations. citeturn0search0turn0search2

Next exact frontier:
1. Freshly validate the normalized adapter source and AB104.131 runtime regression.
2. Audit concurrent effects with different idempotency keys against the same simulation/state revision; same-key serialization is not sufficient if independent keys can mutate the same state concurrently.
3. If required, introduce the smallest state-level serialization or optimistic conflict boundary, then regress it.
4. Continue toward the actual persistence boundary and multi-process CAS/versioning only after in-process state semantics are clean.

DO-NOT-REPEAT:
- Do not assume canonical source is syntactically valid merely because the intended code looks correct.
- Do not claim CI passed without a fresh workflow result.
- Do not equate same-key idempotency with general state concurrency safety.
- Do not claim distributed linearizability from in-process locks.

CONTINUITY:
Resume from AB104.132 at commit 6afacb2e6cf0d62117de82f3548c3fd747bb186f. First obtain fresh validation; then audit different-key concurrent state mutation.
