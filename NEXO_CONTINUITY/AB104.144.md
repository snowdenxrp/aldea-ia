# AB104.144 — canonical assistant/world-state memory loading repair — 2026-09-26

Previous: AB104.143.

## INVESTIGAR
Inspected current `scripts/assistants.mjs`, `scripts/simulate.mjs`, `src/simulation.js`, and restart persistence tests.

Found a concrete defect in the AB104.143 attempt: the assistant workflow wrote `simulation.nexoMemory`, but it still reconstructed the simulation manually from `world-state.json` and did not actually load the persisted `nexoMemory` through the canonical `applyState()` path. The earlier planned `persistState` call had also not been present in the fetched source.

## CONSTRUIR
Commit `18cc64d3f0ce9999a19e280556af65b1f02fe01a` repairs the topology:
- `scripts/assistants.mjs` now imports `loadState`, `applyState`, and `persistState` from `scripts/simulate.mjs`.
- It loads the complete canonical state through `loadState()`.
- It constructs the simulation through `applyState()`, which restores `state.nexoMemory` into `simulation.nexoMemory`.
- The generated Nexo mission is recorded into that canonical Nexo memory.
- The updated simulation is persisted back to `world-state.json` through the revision-checked/locked `persistState()`.
- The assistant-learning memory remains separately stored in `.lumina-assistant-memory.json`.

This closes the previously identified read/write gap at the assistant workflow boundary without merging the two memory stores.

## VALIDATION
- Source was fetched from GitHub after the repair.
- `fetch_commit_workflow_runs` for `18cc64d3f0ce9999a19e280556af65b1f02fe01a` returned no workflow runs.
- Therefore **CI PASS is NOT CLAIMED**.
- No local execution is claimed.

## SEMANTIC STATE
🟢 Persistence topology now uses one canonical world-state path for Nexo memory.
🔵 Assistant-learning memory remains a separate store.
🔵 Prepared-effect recovery remains reconciliation-only.
🟠 Durable external-effect journal is still unresolved.
🔴 No claim of exactly-once physical external effects.
UNKNOWN: distributed/network filesystem semantics.

## PENDING
1. Obtain fresh CI evidence for the post-repair commits.
2. Add an explicit integration test around the assistant workflow's canonical state load/save boundary if testability can be added without executing the production workflow against the repository's real state.
3. Make effect journal durable through the owner of the persistent state, rather than pretending the adapter's injected array is crash durable.
4. Define effect-specific reconciliation contracts.
5. Analyze crash ordering: physical effect before durable completion, durable completion before physical effect, timeout/ambiguous provider response, duplicate delivery.
6. Only after these are closed, revisit stronger distributed/fencing mechanisms.

## DO-NOT-REPEAT
- Do not say assistant Nexo memory is persisted merely because an in-memory object was updated.
- Do not treat `prepared` as proof that an effect did not happen.
- Do not retry prepared external effects without reconciliation.
- Do not claim CI success without a fresh successful run.
- Do not claim the filesystem lock is distributed.
- Do not delete AB104.139–143 evidence.
