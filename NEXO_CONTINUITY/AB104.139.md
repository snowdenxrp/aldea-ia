# AB104.139 — concurrent persistence exposed temp-file collision; repaired and validated — 2026-09-26

Previous: AB104.138.

Fresh adversarial work:
- Added a deterministic synchronization seam around the persistence read/check/write boundary and constructed two writers starting from the same stateRevision=0.
- Run 2283 / 36208010088 on e769306c3f75f0a6023b7da0c20fba63f8c1687e FAILED at simulation-restart-persistence.test.mjs:108.
- Concrete cause: both concurrent writes generated the same temp path because the previous name used only process.pid + Date.now(). Same-process calls occurring in the same millisecond collided on the same temporary file. One rename could consume the file before the other rename, producing a rejected writer. This is a real concurrency defect in the persistence implementation.
- This failure was not the stale-revision race itself.

Repair:
- Commit bdb5cee9cd0ba0d8865575656dc6cc20dc130132.
- Added a per-process tempSequence and included it in the temporary filename: process.pid + Date.now() + sequence.
- This makes concurrent same-process temp files unique even within the same millisecond.

Validation:
- Run 2284 / 36208042249 on bdb5cee9cd0ba0d8865575656dc6cc20dc130132 COMPLETED SUCCESS.
- The adversarial test now permits both writers to pass the same revision check and reach the write phase concurrently; both persistence calls fulfill and final stateRevision remains 1.
- Therefore the temp-file collision is repaired and fresh CI validated.

Critical remaining semantic result:
- The adversarial test intentionally lets both writers observe revision 0 before either write. Both can therefore pass expectedRevision=0 and both can write revision 1.
- The final file contains whichever writer's rename wins. The current stateRevision cannot distinguish the lost update because both wrote revision 1.
- Thus the current expectedRevision mechanism is NOT an atomic compare-and-swap and does NOT prevent simultaneous same-revision last-writer-wins.
- This is now demonstrated by the test structure, not merely inferred.
- No claim of multi-process linearizability is permitted.

Additional historical failure:
- Run 2275 / 36207899494 was cancelled.
- Run 2276 / 36207903987 SUCCESS validated the revision guard before adversarial work.
- Run 2283 / 36208010088 FAILED for the concrete temp-file collision above.
- Run 2284 / 36208042249 SUCCESS validates the temp-file uniqueness repair.

Next exact action:
1. Decide the actual deployment invariant: workflow serialization/rebase may be sufficient for the current scheduled writer, but that must not be confused with a general filesystem guarantee.
2. Research and test a real cross-process coordination primitive suitable for Node/local deployment, including crash/stale-lock behavior, before implementing one.
3. Continue crash-window testing for write failure, rename failure, and interrupted writer.
4. Audit .lumina-assistant-memory.json versus world-state.json/nexoMemory as separate durable stores.
5. Analyze physical-effect-before-ledger versus ledger-before-effect crash semantics.

DO-NOT-REPEAT:
- Do not call expectedRevision atomic CAS.
- Do not erase run 2283's temp collision failure.
- Do not claim the same-revision writer race is solved.
- Do not introduce a lock without testing stale-lock/crash recovery semantics.
- Do not merge the two memory stores conceptually without evidence.

CONTINUITY:
Resume from bdb5cee9cd0ba0d8865575656dc6cc20dc130132 with run 2284 SUCCESS. The next unresolved frontier is genuine cross-process coordination/linearizability, then crash-window and dual-memory consistency.
